# Changelog

## v1.1.0

修复一次严重的数据事故并重写去重与迁移模型。**升级后需手动同步一次**（见下）。

### 🔴 修复：大规模重复文档

446 个 source 的库被放大成 6850 篇文档、990 条高亮变成 16409 个块，且每次自动同步再复制一轮。
根因是思源 `/api/query/sql` 对**没有显式 `LIMIT`** 的语句默认只返回 64 行，不报错也不带截断标记，
残缺的索引被理解成「这些 source 没同步过」，于是重新建档、重灌高亮。

- 索引查询补显式 `LIMIT` 并检测触顶，触顶即中止同步
- 建档前新增两级查找：`getIDsByHPath` 按路径查（零延迟）+ 按 `source-id` 精确点查
- 存在性判据由 `getBlockAttrs` 改为 `getBlockKramdown`——前者对已删除文档**仍长期返回属性**，
  曾导致往已删文档追加块并以 `parent block not found` 整轮失败
- 新增重复建档熔断与 `index_error` 状态：索引可疑时停机报错，不重试、不制造脏数据
- `sourceDocMap` / `knownFolders` 持久化到 `data.json`

### 同步模型

- 增量游标改为**全量对账**：删掉的文档下次同步会自动重建（增量游标永远发现不了本地删除）
- 文档位置改为**每次同步对账**：设置与实际位置不一致就搬，不再依赖「设置变更」事件
- 改文件夹或换笔记本时，已有文档会迁移到新位置（`moveDocsByID` 不改变文档 id，高亮与锚定属性全部保留）
- 翻页对瞬时网络故障重试（4 次，1s/2s/4s 退避）；`forwardProxy` 单次超时 15s → 30s

### ⚠️ 首次同步须手动发起（升级用户同样适用）

目标笔记本与文件夹都有默认值，此前「填个 token 点保存」就足以让插件用一套从没确认过的
目的地往笔记里写。现在新增 `inited` 标记：

- **在你亲自点过一次同步之前，启动同步 / 定时同步 / 保存后同步一律不跑**
- `syncOnStartup` 仍默认开启——它是「想不想开机同步」的偏好，与「初始化完没完」是两件事
- 从 v1.0.0 升级的用户 `inited` 为 `false`，**需要在设置里点一次「保存并立即同步」**才会恢复自动同步

### 设置面板

- 新增「保存并立即同步」按钮与**目的地实时预览**（文档将写入：笔记本 → 文件夹）
- 新增「重置初始化」按钮：思源卸载插件不会删除 `data/storage/petal` 下的数据，
  「删掉重装」并不会回到未初始化状态，因此提供显式重置
- 笔记本下拉新增「请选择笔记本」空占位，不再自动选中列表第一个
- 每个设置项补上常驻说明
- 打开设置时重建面板，未保存的编辑（如 ESC 关闭）不再残留
- 同步失败的提示会说明原因，不再只显示「已延后 60s」

### 工程

- 单测 50 → 129
- 新增 `scripts/kernel-e2e-probe.mts`：用真实网关代码打真实思源内核验证，需本机思源运行，不进 CI
- 设计说明与事故复盘见 `docs/features/SYNC_ENGINE_LOGIC.md`

## v1.0.0

- 首个集市发布版本(功能与 v0.1.0 一致,补充隐私与安全说明)。
- `plugin.json` 收敛 `frontends` 为 `["all"]`。

## v0.1.0

- 初版 Acorny → 思源笔记 高亮同步插件。
- 单向增量同步（游标分页）；一个 source 对应一篇文档。
- 思源原生去重：块自定义属性（`custom-acorny-source-id` / `custom-acorny-id`）+ SQL 查询；编辑保护；原子 IAL 写入。
- 触发：顶栏图标、命令面板、启动时、定时轮询。
- v1 限制：仅追加不回改、不追踪删除、单实例串行幂等、发布模式禁用。
